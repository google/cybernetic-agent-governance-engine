# Copyright 2026 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     https://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-stack-fresh
  namespace: governance-stack
  labels:
    app: redis-stack-fresh
    tier: control-plane
    cage.governance/component: redis-stack-fresh
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-stack-fresh
  template:
    metadata:
      labels:
        app: redis-stack-fresh
        tier: control-plane
        cage.governance/component: redis-stack-fresh
    spec:
      # ── Pod-level security context ────────────────────────────────────────
      # Satisfies PodSecurity "restricted:latest":
      #   • runAsNonRoot=true  (violation 3)
      #   • seccompProfile     (violation 4)
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: redis-stack
          image: redis/redis-stack-server:7.4.0-v1
          args:
            - "redis-server"
            - "/usr/local/etc/redis/redis.conf"
            - "--requirepass"
            - "$(REDIS_PASSWORD)"
            - "--loadmodule"
            - "/opt/redis-stack/lib/rejson.so"
            - "--loadmodule"
            - "/opt/redis-stack/lib/redisearch.so"
          env:
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: redis-credentials
                  key: redis-password
          ports:
            - containerPort: 6379
              name: redis
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
          # ── Container-level security context ───────────────────────────────
          # Satisfies PodSecurity "restricted:latest":
          #   • allowPrivilegeEscalation=false  (violation 1)
          #   • capabilities.drop=["ALL"]       (violation 2)
          #   • runAsNonRoot=true               (violation 3)
          #   • seccompProfile.type=RuntimeDefault (violation 4)
          # runAsUser/runAsGroup set to 999 (redis) because redis-stack-server
          # defaults to root; explicit UID forces non-root execution.
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            runAsNonRoot: true
            runAsUser: 999
            runAsGroup: 999
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - name: config
              mountPath: /usr/local/etc/redis
              readOnly: true
          livenessProbe:
            tcpSocket:
              port: 6379
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            tcpSocket:
              port: 6379
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: config
          configMap:
            name: cage-redis-config
            items:
              - key: redis.conf
                path: redis.conf
---
apiVersion: v1
kind: Service
metadata:
  name: redis-stack-fresh
  namespace: governance-stack
  labels:
    app: redis-stack-fresh
    tier: control-plane
    cage.governance/component: redis-stack-fresh
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
  selector:
    app: redis-stack-fresh
